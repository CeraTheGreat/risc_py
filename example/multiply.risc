'Labels and stack, but still no *real* function calls
PSH 6
PSH 5

MOV [STP] ACC
POP
MOV [STP] BAK

'Multiplication loop
LOOP:
SUB 1
JEZ END
PSH BAK
MOV ACC BAK
MOV [STP] ACC
POP
ADD [STP]
PSH ACC
MOV BAK ACC
MOV [STP] BAK
POP
JMP LOOP
END:
INP BAK
CHP 12
CHP 13
HLT
